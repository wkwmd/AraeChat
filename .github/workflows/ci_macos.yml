name: ci-macos

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  belowc-macos:
    runs-on: macos-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust (stable)
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo + target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Build (release)
        shell: bash
        run: |
          set -euo pipefail
          cargo build -p belowc_bin --release

      - name: Run Core Gates (G-06..G-19)
        id: gates
        shell: bash
        continue-on-error: true
        run: |
          set -euo pipefail
          chmod +x ci/run_all_remaining.sh
          ./ci/run_all_remaining.sh

      - name: X-04 Reproducible Build Identity Check
        shell: bash
        run: |
          set -euo pipefail
          chmod +x ci/x04_repro_check.sh
          ./ci/x04_repro_check.sh
      
      - name: Dump Log Artifacts on UI Failure
        if: steps.gates.outcome == 'failure'
        shell: bash
        run: |
          echo "=== SUMMARY ==="
          cat ci/logs/run_all_summary.txt || true
          echo "=== FIRST FAIL ==="
          for f in ci/logs/g*.txt; do
            if grep -q "STATUS: FAIL" "$f"; then
              echo "=> $f"
              head -n 30 "$f" || true
              break
            fi
          done
          exit 1

      - name: Upload ci logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-logs-macos
          path: ci/logs

      - name: Upload ci artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-artifacts-macos
          path: ci/artifacts
