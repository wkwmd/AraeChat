# Gate: G-01 — SSOT Path Seal

## SSOT anchors
- spec/ssot.yaml: canon.ssot_file_path_sealed

## Goal
SSOT 파일이 `spec/ssot.yaml` 경로에 존재하며, SSOT 내부의 경로 봉인값이 동일함을 증명한다.

## Checks (must pass)
- [ ] `spec/ssot.yaml` 파일이 존재한다.
- [ ] `ssot.canon.ssot_file_path_sealed == "spec/ssot.yaml"` 이다.
- [ ] (권장) `spec/SSOT.md`가 존재하고 SSOT 포인터로서 `spec/ssot.yaml`을 가리킨다.

## Test vectors
- SSOT 파일 존재: `spec/ssot.yaml`
- SSOT 내부 필드 검사: `ssot.canon.ssot_file_path_sealed`
- 포인터 파일 검사(권장): `spec/SSOT.md`

## Evidence artifacts (paths)
- build log: N/A
- symbol scan: N/A
- run logs: ci/logs/g01_ssot_path.txt
- outputs: N/A

## Notes
- 이 Gate는 코드 실행이 아니라 “명세 파일의 단일 원천 경로 봉인”을 증명한다.
