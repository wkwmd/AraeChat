# Gate: G-15 — E1b streaming & no accumulation

## SSOT anchors
- spec/ssot.yaml: e1b_streaming_accum_emit.streaming
- spec/ssot.yaml: e1b_streaming_accum_emit.streaming.accumulation_forbidden

## Goal
컴파일이 1-pass 스트리밍이며 입력/상태/출력 누적이 없음을 증명한다.

## Checks (must pass)
- [ ] 소스 감사: Vec/collect/String/format/println 등 누적/출력 유발 토큰이 금지 영역에서 발견되지 않는다.
- [ ] 런타임 스모크: 큰 입력에서도 메모리 폭증/버퍼링 동작이 나타나지 않는다(가능한 범위에서).

## Test vectors
- 작은 입력: 1~2줄
- 큰 입력: 긴 파일(라인 수/길이 증가) — 결과는 동일 규칙으로 생성
- 실패 입력: 중간에 SyntaxError가 나도 out 오염 없이 종료(atomic gate와 연동)

## Evidence artifacts (paths)
- build log: N/A
- symbol scan: (옵션) ci/logs/g15_symbols_runtime.txt
- run logs: ci/logs/g15_streaming_audit.txt
- outputs: N/A

## Notes
- “무누적”은 완전 자동 증명이 어렵기 때문에 (1) 금지 API 감사 + (2) 통합 증거로 접근한다.
