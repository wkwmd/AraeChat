# Gate: G-05 — ZH No alloc crate

## SSOT anchors
- spec/ssot.yaml: zero_heap_no_dynmem_gate.rust.alloc_crate_forbidden

## Goal
`alloc` 크레이트가 링크/사용되지 않음을 증명한다.

## Checks (must pass)
- [ ] 빌드 산출물(바이너리/오브젝트/심볼)에서 `alloc::` 흔적이 없다.
- [ ] Rust 전역 할당자 관련 심볼(예: __rust_alloc 계열)이 없다.

## Test vectors
- 대상: 최종 바이너리 또는 .obj/.rlib
- 패턴: `alloc::`, `__rust_alloc`, `__rg_alloc` 등
- 기대: 0건

## Evidence artifacts (paths)
- build log: N/A
- symbol scan: ci/logs/g05_no_alloc.txt
- run logs: N/A
- outputs: N/A

## Notes
- 이 Gate는 “동적 메모리 미사용”의 전제 조건을 강하게 만든다.
