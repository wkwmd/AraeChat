# Gate: G-03 — ZH Build no_std/no_main

## SSOT anchors
- spec/ssot.yaml: zero_heap_no_dynmem_gate.rust.no_std
- spec/ssot.yaml: zero_heap_no_dynmem_gate.rust.no_main

## Goal
Windows 1차 타겟에서 바이너리가 `no_std`/`no_main` 구성으로 빌드됨을 증명한다.

## Checks (must pass)
- [ ] Windows에서 release 빌드가 성공한다.
- [ ] 빌드 로그(또는 rustc 인자/출력) 상 `no_std`, `no_main` 조건을 만족함을 확인할 수 있다.
- [ ] panic=abort 구성이 적용된다(권장: profile 설정/링크 결과로 확인).

## Test vectors
- 빌드: release / Windows CI
- 기대: build OK
- 기대: 로그에 no_std/no_main 단서 존재

## Evidence artifacts (paths)
- build log: ci/logs/g03_build.txt
- symbol scan: N/A
- run logs: N/A
- outputs: (옵션) ci/artifacts/g03_binary_info.txt

## Notes
- “어떤 엔트리포인트로 링크되었는지”는 후속 Gate에서 더 구체화해도 됨.
