# Gate: G-02 — SSOT YAML Parse

## SSOT anchors
- spec/ssot.yaml: (entire file)

## Goal
CI가 SSOT YAML을 오류 없이 파싱/로드할 수 있음을 증명한다.

## Checks (must pass)
- [ ] YAML 파서가 `spec/ssot.yaml` 로드에 성공한다.
- [ ] 로드된 트리에서 최상위 키 `ssot`가 존재한다.

## Test vectors
- 입력: `spec/ssot.yaml`
- 기대: parse OK
- 기대: root key `ssot` 존재

## Evidence artifacts (paths)
- build log: N/A
- symbol scan: N/A
- run logs: ci/logs/g02_ssot_parse.txt
- outputs: N/A

## Notes
- YAML 문법 오류/인덴트 깨짐은 여기서 즉시 차단한다.
