# Gate: G-04 — ZH No External Crates

## SSOT anchors
- spec/ssot.yaml: zero_heap_no_dynmem_gate.rust.external_crates

## Goal
외부 크레이트 의존성 0을 증명한다(표준/자체 제외).

## Checks (must pass)
- [ ] 의존성 메타데이터에서 외부 크레이트 count == 0.
- [ ] (권장) Cargo.toml/Cargo.lock 검사로 서드파티가 없음을 확인.

## Test vectors
- 입력: Cargo 메타데이터(Cargo.lock 또는 cargo metadata 출력)
- 기대: external crates == 0
- 기대: forbidden crates 목록 없음

## Evidence artifacts (paths)
- build log: N/A
- symbol scan: N/A
- run logs: ci/logs/g04_deps.txt
- outputs: N/A

## Notes
- “core”는 no_std에서 필수이므로 외부로 카운트하지 않는다.
