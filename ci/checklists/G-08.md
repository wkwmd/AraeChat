# Gate: G-08 — CLI Contract positional 2 args, no flags

## SSOT anchors
- spec/ssot.yaml: cli_process_contract.cli.usage
- spec/ssot.yaml: cli_process_contract.cli.argv.required_args
- spec/ssot.yaml: cli_process_contract.cli.argv.flags_supported

## Goal
CLI가 정확히 `belowc <in> <out>`만 허용함을 증명한다(플래그/옵션 없음).

## Checks (must pass)
- [ ] 인자 0개/1개/3개 이상: exit == 1, stderr_len == 0
- [ ] `-o` 등 플래그 포함: exit == 1, stderr_len == 0
- [ ] 정확히 2개 인자(입력 경로, 출력 경로): 정상 경로로 진입(후속 Gate에서 결과 검증)

## Test vectors
- `belowc` → fail
- `belowc in.txt` → fail
- `belowc in.txt out.bin extra` → fail
- `belowc -o out.bin in.txt` → fail
- `belowc in.txt out.bin` → allowed (success/failure는 입력 내용에 따름)

## Evidence artifacts (paths)
- build log: N/A
- symbol scan: N/A
- run logs: ci/logs/g08_cli_matrix.txt
- outputs: N/A

## Notes
- “allowed 케이스”는 이후 gate에서 컴파일 결과로 합격을 판단한다.
