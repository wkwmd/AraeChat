# Gate: G-07 — ZH stderr == 0 bytes

## SSOT anchors
- spec/ssot.yaml: zero_heap_no_dynmem_gate.stderr_bytes_must_be
- spec/ssot.yaml: cli_process_contract.stderr_bytes_must_be

## Goal
성공/실패 모든 케이스에서 stderr 출력이 0바이트임을 증명한다.

## Checks (must pass)
- [ ] 성공 케이스 실행 시 stderr_len == 0
- [ ] 실패 케이스 실행 시 stderr_len == 0
- [ ] stderr 대신 stdout에 진단을 출력하지 않는다(권장: 실패 케이스 stdout도 0).

## Test vectors
- 성공 케이스: 최소 유효 입력(한 줄, 허용 음절 포함) → exit 0
- 실패 케이스: 금지 문자 포함 또는 argv 잘못 → exit 1
- 기대: stderr always empty

## Evidence artifacts (paths)
- build log: N/A
- symbol scan: N/A
- run logs: ci/logs/g07_stderr.txt
- outputs: N/A

## Notes
- 이 Gate가 PASS되면, 이후 모든 Gate 스크립트는 “오류 메시지”가 아니라 로그 파일로만 판정해야 한다.
